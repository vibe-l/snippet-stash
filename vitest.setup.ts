// This file can be used to set up any global configurations or mocks for Vitest.
// For now, it's empty, but you can add configurations here as needed.

// Example: Mocking a global function
// global.myGlobalFunction = vi.fn(() => 'mocked value');

// Example: Setting up MSW
// import { server } from './mocks/server';
// beforeAll(() => server.listen());
// afterEach(() => server.resetHandlers());
// afterAll(() => server.close());
